# AI 远程控制工具 — 初始设计文档

> 基于 `样例代码/`（Localaw）改造；C2 / 远程端双端架构，网络通信桥接大模型对话与命令执行。

---

## 1. 设计目标

将原"本地 AI 助手"重构为 **C2 / Agent 远程架构**，由大模型在 C2 端解析用户意图，按指定目标下发指令至远程端执行，结果回收后反馈给大模型继续推理。

**与原代码的本质区别**：原代码中 `ModelModule` 与 `CommandModule`/`FileModule` 在同一进程；新版通过 TCP + 自定义二进制协议将两者解耦到不同主机。

---

## 2. 架构总览

```
┌─────────────────────────────────────────────────────────────────┐
│                       C2 端（C2端）                              │
│                                                                 │
│   ┌────────┐    ┌──────────┐    ┌────────────┐    ┌──────────┐ │
│   │  CLI   │    │   Web    │    │   Model    │    │  LLM     │ │
│   │ 主循环 │←→│  Server   │←→│  Module    │←→│  Client   │ │
│   └────────┘    └──────────┘    └─────┬──────┘    └──────────┘ │
│                                       │                         │
│                              ┌────────┴────────┐                │
│                              │ AgentRegistry   │                │
│                              │ (多远程端状态)   │                │
│                              └────────┬────────┘                │
│                                       │                         │
│                              ┌────────┴────────┐                │
│                              │  Forwarder      │                │
│                              │ (指令转发+等待)   │                │
│                              └────────┬────────┘                │
│                                       │                         │
│                              ┌────────┴────────┐                │
│                              │ NetworkServer   │                │
│                              │ (TCP 监听)       │                │
│                              └─────────┬───────┘                │
└──────────────────────────────────────┼──────────────────────────┘
                                       │  二进制协议
                                       │
┌──────────────────────────────────────┼──────────────────────────┐
│                       远程端（Agent） │                          │
│                              ┌───────┴───────┐                  │
│                              │ AgentClient   │                  │
│                              │ (主动拨号)     │                  │
│                              └───────┬───────┘                  │
│                                      │                          │
│                              ┌───────┴───────┐                  │
│                              │   Handler     │                  │
│                              │ 指令解析+分发  │                  │
│                              └───────┬───────┘                  │
│                                      │                          │
│                              ┌───────┴───────┐                  │
│                              │ LocalExecutor │                  │
│                              │ 文件+Shell 执行│                  │
│                              └───────────────┘                  │
└─────────────────────────────────────────────────────────────────┘
```

**关键边界**：

| 关注点     | C2 端                | 远程端              |
| ---------- | -------------------- | ------------------- |
| LLM 调用   | ✅                   | ❌                  |
| 授权/鉴权  | ✅（用户授权、Agent 认证） | ❌              |
| 安全检查   | ✅（黑名单、命令过滤）  | ❌（信任 C2 指令） |
| 指令执行   | ❌                   | ✅                  |
| 文件操作   | ❌                   | ✅                  |
| 协议编解码 | ✅                   | ✅                  |
| 会话历史   | ✅（每 Agent 一份）   | ❌（无状态）        |
| 心跳保活   | ✅（被动接收）        | ✅（主动发送）      |

---

## 3. 网络通信协议

### 3.1 包结构总览

```
┌─────────────────────── 16 字节包头 ───────────────────────┐
│  request_id (uint64 LE, 8B) │ body_len (uint32 LE, 4B) │ R │cmd│
│                             │                         │3B│1B │
├──────────────────────── 包身（请求/响应格式不同） ─────────┤
```

- **包头**（固定 16 字节）：
  - `request_id`（8B，little-endian uint64）：请求标识；响应包必须回填发起方的 request_id
  - `body_len`（4B，little-endian uint32）：包身字节数，`0` 表示无包身
  - `reserved`（3B）：保留字段，必须置 `0`
  - `cmd`（1B，uint8）：指令码（见 §3.3）
- **包身格式**：
  - **请求（C2 → Agent）**：TLV 链式结构。`uint32 length` + UTF-8 数据，多组串接。
  - **响应（Agent → C2）**：**单一结构**，整段 UTF-8 字符串即为指令执行返回结果，不再切分、不带 4 字节长度前缀。

> request_id 由 C2 端为每条指令生成，全局唯一（递增即可）；Agent 在响应中**原样回填**，C2 据此匹配请求与响应。

### 3.2 大小端

整数字段一律 **little-endian**。

### 3.3 指令码分配

#### 动作指令（C2 → Agent，Agent → C2 同号回包）

| cmd  | 名称        | 包身参数顺序（TLV 链）                                  |
| ---- | ----------- | ------------------------------------------------------- |
| 0x01 | list_dir    | `path`                                                  |
| 0x02 | make_dir    | `path`                                                  |
| 0x03 | delete_dir  | `path`                                                  |
| 0x04 | rename_dir  | `path`, `new_name`                                      |
| 0x05 | read_file   | `path`, `start_line`, `end_line`                        |
| 0x06 | write_file  | `path`, `content`                                       |
| 0x07 | delete_file | `path`                                                  |
| 0x08 | edit_file   | `path`, `operation`, `start_line`, `end_line`, `content` |
| 0x09 | rename_file | `path`, `new_name`                                      |
| 0x0A | copy        | `src`, `dest`                                           |
| 0x0B | move        | `src`, `dest`                                           |
| 0x0C | upload      | `dest_path`（详见 §3.6）                                |
| 0x0D | download    | `src_path`（详见 §3.6）                                 |
| 0x0E | create_file | `path`                                                  |
| 0x0F | get_cwd     | （无参数）                                              |
| 0x10 | exec_cmd    | `command`                                               |

> **大小限制**：`read_file`（0x05）整文件读取时**截断至 50000 字符**，复用样例代码 `file.py:77` 的限制；超长文件由 LLM 通过 `start_line`/`end_line` 分段读取。`write_file`/`edit_file` 的 content 受 LLM 上下文窗口与 JSON 命令长度自然约束。`upload`/`download` 数据包固定 1024 字节/包，无单包限制。

#### 控制指令

| cmd     | 方向       | 名称              | 包身参数                              |
| ------- | ---------- | ----------------- | ------------------------------------- |
| 0x80    | Agent → C2 | register          | `nonce`（随机字符串，仅此一项；不含任何身份信息）|
| 0x81    | C2 → Agent | register_response | `sha256(nonce + c2_auth_tokens)`（十六进制哈希，单一字符串包身）|
| 0x82    | Agent → C2 | heartbeat         | `timestamp`（秒，UTF-8 数字串）       |
| 0x83    | C2 → Agent | heartbeat_ack     | `timestamp`                           |
| 0x84    | 双向       | disconnect        | `reason`                              |
| 0x85    | C2 → Agent | shutdown          | `reason`（关闭远程端进程，非系统关机）|
| 0x86    | Agent → C2 | register_confirm  | `agent_id`, `hostname`, `os`（挑战验证成功后的最终确认；身份信息在此时才上报）|

> **注册不再明文传输 token，且认证前不泄露身份**：`register` **仅携带随机 nonce**（不含 agent_id / hostname / os）；`register_response` 返回挑战哈希；身份信息（agent_id / hostname / os）**延后到验证成功后的 `register_confirm` 才上报**。任一环节失败（哈希不匹配 / 超时 / 断连 / 包序异常），C2 端不注册并关闭连接，不做任何其他操作。

#### 错误码与错误分类

C2 端在处理 Agent 响应时，必须区分**业务错误**与**网络错误**：

| 类别     | 含义                                     | Agent 侧行为                       | C2 侧行为                                                              | 是否进入 LLM 会话历史 |
| -------- | ---------------------------------------- | ---------------------------------- | ---------------------------------------------------------------------- | --------------------- |
| 成功     | 指令正常完成                             | 正常返回结果字符串                 | 透传给 LLM 作为"执行结果"                                              | ✅                    |
| 业务错误 | 指令已送达并执行，但失败（文件不存在等） | 返回以 `Error: ...` 开头的字符串   | 透传给 LLM 作为"执行结果"，由 LLM 决定后续策略                        | ✅                    |
| 网络错误 | 指令未送达 / Agent 无响应 / 连接断开     | 不适用                              | **不写入 LLM 会话**；Forwarder 抛特定异常；ModelModule 中断当前多轮循环 | ❌                    |

**网络错误的具体触发条件**（C2 侧 `Forwarder` 内部判定）：

- Agent TCP 连接在请求发出后断开
- 等待响应超时（`cmd_timeout`）
- 响应包包头解析失败（协议异常，连接一并关闭）
- 响应包 `request_id` 与本地 pending 队列中任何 id 都不匹配（视为非法响应）

**C2 侧处理流程**（伪代码）：

```python
async def forward(cmd, params):
    req_id = next_request_id()
    future = register_pending(req_id)             # 注册 Future
    await send_packet(req_id, cmd, params)
    try:
        result = await asyncio.wait_for(future, timeout=cmd_timeout)
        return result                               # 业务结果（含 "Error:..." 业务错误）
    except asyncio.TimeoutError:
        raise NetworkError("timeout")               # 网络错误
    except ConnectionLost:
        raise NetworkError("disconnected")
    finally:
        unregister_pending(req_id)
```

**ModelModule 中的网络错误处理**：

- 网络错误抛出后，当前多轮对话循环立即终止
- 向 LLM 会话历史插入一条**系统消息**：`[Network Error: <reason>. Agent <id> disconnected.]`，提示 LLM 调整策略
- 若 AgentRegistry 标记该 Agent 离线，CLI/Web 顶部下拉框对应项灰显
- `/target` 切换到其他在线 Agent 继续操作；如无可用 Agent，提示用户重连

> "系统消息"插入会话历史而非业务结果，保证 LLM 能感知网络中断但不会污染"指令执行结果"序列。

### 3.4 请求-响应匹配

每个动作请求由 C2 端生成全局唯一的 `request_id`（uint64 递增即可），写入**包头**的 8 字节字段发出。Agent 收到后**原样回填**至响应包头，C2 端按 `request_id` 索引到对应 `Future`，将响应包身（单一结果字符串）作为 `Future` 结果。

- 控制包（register / heartbeat / disconnect 等）同样携带 `request_id`，便于异步应答路由
- 若 Agent 端收到请求后 `request_id` 解析失败，应直接关闭连接（视为协议异常）

### 3.5 Agent 注册握手

Agent 的身份信息（`agent_id` / `hostname` / `os`）在 **TCP 连接建立后立即通过 `register` 包上报**，C2 据此建立 `AgentRegistry` 条目。完整流程：

```
Agent                              C2
  │── TCP SYN ───────────────────────▶│
  │◀─────── TCP SYN+ACK ──────────────│
  │──────── TCP ACK ─────────────────▶│   (TCP 建立)
  │                                   │
  │── register(req_id=1, nonce) ─────▶│   cmd=0x80
  │                                   │   TLV: 仅随机字符串（不含任何身份信息）
  │                                   │
  │                                   │   → digest = sha256(nonce + c2_auth_tokens)
  │◀────── register_response ─────────│   cmd=0x81
  │     req_id=1, digest              │   单一字符串包身 = 十六进制哈希
  │                                   │
  │  （远端本地校验：                   │
  │   sha256(nonce + auth_token)       │
  │   == digest ? 继续 : 断开）         │
  │── register_confirm(req_id=2, ────▶│   cmd=0x86
  │     agent_id, hostname, os)       │   TLV: agent_id, hostname, os
  │                                   │   → AgentRegistry.register(info)
  │                                   │
  │── heartbeat(req_id=3, ts) ───────▶│   (周期性)
  │◀── heartbeat_ack(req_id=3, ts) ───│
  │                                   │
  │── exec_cmd(req_id=N, command) ───▶│   业务请求
  │◀── exec_cmd(req_id=N, result) ────│   业务响应
  │                                   │
  │── disconnect(req_id=N, reason) ──▶│   主动下线 / C2 侧超时关闭
```

- `register` 仅携带随机 nonce，**不含任何身份信息**；身份信息（`agent_id` / `hostname` / `os`）在验证成功后的 `register_confirm` 中才上报；`register_response` 仅回传挑战哈希，不注册
- `hostname` / `os` 仅用于 C2 端展示与 system_prompt 注入（`{system_name}`），不参与鉴权
- 心跳包仅维持连接活性，不携带业务信息
- 验证失败（哈希不匹配 / 未收到 `register_confirm` / 握手超时）时，C2 端不注册并关闭该 TCP 连接，不做任何其他操作
- **`register_confirm` 之后**，该连接上的全部流量切换为 ChaCha20 加密（见 §3.7）

### 3.6 文件传输协议（upload / download）

文件传输分**两阶段**：初始化指令包 + 数据包序列。两阶段共享同一个 `request_id`，便于接收端将数据包关联到具体传输任务。

#### 3.6.1 阶段一：初始化指令包

- 使用现有动作指令码：`upload`（0x0C）或 `download`（0x0D）
- 包身 TLV：`path`（仅一个参数）
  - `upload`：路径为 Agent 上的**目标保存路径**
  - `download`：路径为 Agent 上的**待读取文件路径**
- C2 端在收到响应前阻塞等待（与其它动作指令一致）

#### 3.6.2 阶段二：数据包

发送方按 1024 字节/包切分文件内容，循环发送**数据包**。数据包包头复用 16 字节结构，但 `cmd` 字节字段含义**改为结束标记**：

```
┌─────────────────────── 16 字节数据包头 ──────────────────────┐
│  request_id (uint64 LE, 8B, 与初始化包一致) │ body_len (4B) │ R │E│
│                                            │ ≤ 1024        │3B│1B│
├─────────────────────── 包身（≤ 1024 字节） ───────────────────┤
│  file_data (原始字节，UTF-8 透明)                              │
└────────────────────────────────────────────────────────────────┘
```

- `request_id`（8B）：**与初始化包完全相同**
- `body_len`（4B）：本包文件数据字节数，`1 ≤ body_len ≤ 1024`
- `reserved`（3B）：置 `0`
- `end_flag`（1B，原 cmd 字段位置）：`0` = 还有后续包，`1` = 最后一包
- 包身：**原始文件字节**（非 UTF-8 字符串、二进制安全）

#### 3.6.3 完整流程（v1 简化握手版）

为减少握手往返，整个传输过程采用**单次响应**确认机制：

**upload（C2 → Agent）**：
```
C2                                       Agent
 │── upload(req_id=N, dest_path) ───────▶│   Agent 预创建目标文件，进入接收状态
 │── data(req_id=N, end=0, ≤1024B) ─────▶│
 │── data(req_id=N, end=0, ≤1024B) ─────▶│   循环追加写入
 │── data(req_id=N, end=1, ≤1024B) ─────▶│   最后一包
 │                                       │   Agent：关闭文件，落盘
 │◀── response(req_id=N, "Saved ... OK") ─│   最终结果回传
```

**download（Agent → C2）**：
```
C2                                       Agent
 │── download(req_id=N, src_path) ──────▶│   Agent 打开文件，开始分块发送
 │                                       │
 │◀── data(req_id=N, end=0, ≤1024B) ─────│
 │◀── data(req_id=N, end=0, ≤1024B) ─────│   循环接收
 │◀── data(req_id=N, end=1, ≤1024B) ─────│   最后一包
 │  （C2：按 basename 保存到程序目录）     │
 │── response(req_id=N, "Received OK") ──▶│   C2 主动确认
```

- upload 阶段：Agent 在收到 `end_flag=1` 后**主动回传一次**响应
- download 阶段：C2 在收完 `end_flag=1` 后**主动回传一次**响应
- 两次响应均使用 `request_id=N`（与初始化/数据包一致），便于双方路由至同一 Future

#### 3.6.4 边界与错误处理

| 场景                       | 处理                                          |
| -------------------------- | --------------------------------------------- |
| `path` 不存在 / 权限不足   | Agent 立即响应 `Error: ...`，不进入数据阶段   |
| 数据传输中连接断开         | 双方均按传输失败处理；Agent 删除已创建的零字节/半成品文件 |
| `end_flag` 损坏 / 顺序错乱 | C2/Agent 检测到非预期值则关闭连接视为协议异常 |
| 传输超时（默认 30s 无包）  | Agent 主动断开；C2 Forwarder 抛 `TimeoutError` |
| 文件大小超过阈值（建议 100MB）| Agent 端可拒绝并返回 `Error: file too large` |

#### 3.6.5 LLM 触发限制

**upload / download 不通过 LLM JSON 命令触发**——属于 C2 端管理动作，仅由用户在 CLI/Web 显式发起：

- CLI：`/upload <local_path> <dest_path>`、`/download <src_path>`（均作用于当前激活 Agent，详见 §4.4）
- Web：FileManager 面板的"上传到远程 / 从远程下载"按钮

下载时，C2 端将文件保存至**自身程序所在目录**，文件名取源路径的 basename（无需指定目录）。

---

### 3.7 流量加密（ChaCha20，认证通过后启用）

**注册握手 + 认证通过后，连接上的全部后续字节流使用 ChaCha20（RFC 7539：256-bit 密钥 / 96-bit nonce / 32-bit 计数器）流式加密**。握手过程本身（`register` / `register_response` / `register_confirm`）保持明文，以便完成挑战-响应认证。

- **密钥**：`key = SHA-256(auth_token)`（32 字节），由两端各自本地计算，不在线上传输；任意长度 token 均归一化到 32 字节
- **双向独立 nonce**：两条方向的 keystream 互不重叠
  - `C2 → Agent`：nonce = `00 00 00 00 00 00 00 00 00 00 00 00`
  - `Agent → C2`：nonce = `01 00 00 00 00 00 00 00 00 00 00 00`
- **作用范围**：加密作用于**整个字节流**（16B 包头 + 包身 + 文件数据包），接收端先解密再按包头分帧解析；流式计数随字节推进，天然支持任意粘包/半包边界
- **实现**：
  - Python：`common/crypto.py` 纯 Python ChaCha20 + `EncryptedStream`（透明加密写 / 解密读），C2 与 Python 远程端握手成功后分别构造 `tx`/`rx` 两个上下文
  - C 版远程端：`remote-c/protocol.c` 自包含 ChaCha20 + 连接级全局加密态，`send_all` / `recv_more` 内统一加解密；认证成功后 `crypto_enable_agent(key)` 启用
- **兼容性**：两端实现算法一致（Python 经 RFC 7539 §2.3.2 测试向量验证；C 逻辑经跨语言逐字节对拍一致），可互相通信
- **边界**：握手期间若握手包与首个加密包被 TCP 合并到达，接收端需先解密缓冲区内残留字节再分帧（`EncryptedStream.absorb_leftover`）

---

## 4. C2 端设计

### 4.1 进程入口

- CLI 模式：`python -m src.main --mode cli --config config_c2.json`
- Web 模式：`python -m src.main --mode web --config config_c2.json`

> **Web 与 CLI 仅存在于 C2 端**。远程端为无头进程，无任何本地人工管理接口（无本地 CLI、无本地 Web、无文件/命令面板）。所有对远程端的操作都必须经由 C2 → 协议 → 远程端执行。

### 4.2 模块清单（相对样例代码）

| 模块              | 位置           | 改造内容                                                   |
| ----------------- | -------------- | ---------------------------------------------------------- |
| `config.py`       | 复用 + 扩展    | 新增 `c2_host`、`c2_port`、`c2_auth_tokens`（单值）等字段   |
| `controller.py`   | 改造           | 持有 `AgentRegistry`、`Forwarder` 引用；初始化时注入 Agent 列表到 system_prompt |
| `llm.py`          | 几乎原样       | 不变                                                       |
| `model.py`        | 重大改造       | 会话管理改为**按 Agent 维度**：`dict[agent_id, history]`；每轮 chat 根据当前激活 agent 取/写 history |
| `command.py`      | 简化为路由器   | 仅保留"JSON → 协议包 → Forwarder → 等待结果 → 回填 LLM"的编排；删除直接执行逻辑；保留 `check_safety` |
| `file.py`         | **下移到远程端** | 不再被 C2 直接 `import`                                  |
| `web_server.py`   | 改造           | 顶部新增"当前 Agent + 切换下拉"；新增 `/api/agents`（列表）、`/api/agents/switch`（POST body: agent_id）；文件管理 `/api/files/...` 全部针对当前激活 Agent，无需 agent_id 参数 |
| `main.py`         | 改造           | 新增 `/target <agent_id>`、`/agents`、`/auth`、`/reset`、`/upload`、`/download` 命令 |

### 4.3 新增模块

#### `src/c2/network_server.py`
- 基于 `asyncio.start_server`（与 FastAPI 异步共存）
- 每个连接 = 一个 Agent；接收包头 → 包身 → 解析 `cmd`
- 注册/心跳包走特殊分支；动作包按 `agent_id` 路由到该 Agent 的请求队列
- 心跳超时（默认 60s × 3）断开连接并通知 `AgentRegistry`

#### `src/c2/agent_registry.py`
- 维护 `dict[agent_id, AgentInfo]`，其中 `AgentInfo` 包含：
  - `id`、`hostname`、`os`、`connected_at`、`last_heartbeat`
  - 关联的 `asyncio.StreamWriter`、`conversation_history: list`
- 提供 `register(info)`、`unregister(id)`、`get(id)`、`list_all()`
- **`register(info)` 仅由 NetworkServer 在收到 `register` 包（cmd 0x80）并通过 `auth_token` 校验后调用**——这是 Agent 身份信息进入 C2 的唯一入口
- 状态变更时通知 UI（CLI / Web）

#### `src/c2/forwarder.py`
- `async forward(cmd, params) -> str`
- 隐式使用 ModelModule 持有的"激活 Agent"对应的 StreamWriter
- 构造包（包头 + TLV）→ 写入 → 注册一个 `Future` 到 AgentInfo 的 pending 队列
- `await future`，超时（与 `cmd_timeout` 一致）抛 `TimeoutError`
- 结果字符串原样返回

#### `src/c2/file_transfer.py`
- `async upload(local_path, dest_path) -> str`：读取本地文件 → 发送 `upload` 初始化包 → 按 1024B 分块发送数据包（end_flag）→ 等待当前激活 Agent 最终响应
- `async download(src_path) -> str`：发送 `download` 初始化包 → 循环接收数据包并写入 C2 程序目录（`os.path.basename(src_path)`）→ 完成后向当前激活 Agent 发送确认响应

> 两个函数均**隐式使用**当前激活 Agent；调用方需通过 `/target` 先切换至目标。模块不暴露 agent_id 参数。
- 数据包发送/接收需在 NetworkServer 的 per-connection 任务中串行执行，避免与同一连接上的普通动作包交错

### 4.4 CLI 多 Agent 切换

**所有非切换类命令均作用于"当前激活会话对应 Agent"**——不支持任何跨会话 / 跨 Agent 的指令操作。`/target` 是切换激活会话的唯一入口。

| 命令                                  | 作用                                                |
| ------------------------------------- | --------------------------------------------------- |
| `/agents`                             | 列出所有已连接 Agent（id、host、os、在线时长）      |
| `/target <agent_id>`                  | 切换当前激活 Agent（同时切换对应 LLM 会话）         |
| `/auth`                               | 显示当前授权模式 / 切换                             |
| `/reset`                              | 仅重置**当前激活 Agent**的会话历史                 |
| `/upload <local_path> <dest_path>`    | 上传本地文件至**当前激活 Agent**（详见 §3.6）       |
| `/download <src_path>`                | 从**当前激活 Agent**下载文件，保存到 C2 程序目录    |

> 用户需先 `/target <agent_id>` 切到目标 Agent，再执行 `/upload` / `/download`。命令本身不接受 agent_id 参数——避免误操作或越权。

切换语义：
- 切换前自动保存当前激活 Agent 的 history
- 切换后从新 Agent 的 history 继续；首次为新 Agent 发送消息时自动初始化 system_prompt（带 Agent 上下文）

### 4.5 Web 界面增强

- 顶部导航栏右侧新增"目标 Agent"下拉选择器
- 侧边栏（可选）显示所有 Agent 卡片（在线/离线状态、最后一次心跳）
- 切换 Agent 后对话历史区按对应 Agent 的历史渲染
- 文件管理（`FileManager`）面板改为针对当前激活 Agent：所有文件操作通过协议转发至远程端

### 4.6 鉴权

- **挑战-响应握手**（token 不再明文传输）：远程端注册时发送**仅含随机 nonce** 的 `register` 包（不携带 agent_id / hostname / os，认证前不泄露身份）；C2 计算 `digest = sha256(nonce + c2_auth_tokens)` 并回传；远程端用本地 `auth_token` 做同样计算并比对，一致则回传 `register_confirm`（**此时才附带** agent_id / hostname / os），C2 据此注册
- `c2_auth_tokens` 在配置文件中**只允许配置一个值**（`"c2_auth_tokens": "single-token"`）；若配置为多元素列表，C2 启动时报错拒绝
- 校验失败或未收到 `register_confirm`（超时/断连/包序异常）→ C2 不注册、关闭连接、不做任何其他操作
- **认证通过后流量加密**：连接切换为 ChaCha20（§3.7），密钥为 `SHA-256(auth_token)`，握手阶段的注册/挑战/确认保持明文
- **C2 Web 服务自身**仍保持 `127.0.0.1` 默认监听，不引入 Web 鉴权（沿用样例代码"本地使用"约定；如需公网访问再加 Basic Auth / Token）

### 4.7 JSON 命令格式扩展

**与样例代码保持完全一致：LLM 输出的 JSON 命令中不出现任何远程端相关信息**（无 `target`、无 `agent_id` 等字段）。

```json
{"action": "exec_cmd", "command": "systemctl status nginx"}
```

**路由机制**：
- 每个 LLM 会话在创建时即与某个 Agent **1:1 绑定**
- 会话 → Agent 的映射由 C2 端维护（`AgentRegistry` 中对应 `conversation_history`）
- LLM 在某会话内返回的指令，C2 自动转发给该会话**对应**的 Agent 执行
- **不支持**通过命令字段指定其他 Agent；如需切换目标，必须由用户手动切换激活会话（见 §4.4）

**system_prompt 占位符**：

| 占位符         | 替换内容                                       |
| -------------- | ---------------------------------------------- |
| `{system_name}` | 当前**激活会话对应 Agent** 的 OS              |

> 提示词中**不出现 Agent 的 id 与 host**，仅注入目标操作系统信息，使 LLM 推荐命令时贴合目标系统；切换会话后占位符重新求值。

**实现要点**：
- `CommandParser`（`src/llm.py`）**无须任何改造**，继续按原样解析 JSON
- `model.py` 中每条命令的执行结果写入**当前激活会话对应 Agent** 的 `conversation_history`
- `forwarder.forward(cmd, params)` 不再需要 `agent_id` 参数；由 `ModelModule` 根据激活会话隐式决定目标 Agent

---

## 5. 远程端（Agent）设计

### 5.1 进程入口

```bash
# 配置文件方式
python -m remote.main --config config_remote.json

# 纯命令行方式（无需任何文件）
python -m remote.main \
    --c2-address 192.168.1.100:8881 \
    --agent-id server-01 \
    --auth-token token-for-server-01 \
    --heartbeat-interval 30 \
    --cmd-timeout 60

# 混合方式：配置文件提供默认值，命令行覆盖
python -m remote.main --config base.json --c2-address other:8881
```

后台运行（建议）：`nohup python -m remote.main ... &` 或后续加 systemd / Windows Service 封装。

> 远程端**仅此一个入口**，为无头守护进程：启动 → 拨号 → 心跳 → 处理协议包。**无任何交互式 CLI / Web 界面**。所有对远程端的操作（包括文件浏览、命令执行、查看历史）都必须经由 C2 端进行。

### 5.2 模块清单

| 模块                    | 来源         | 说明                                |
| ----------------------- | ------------ | ----------------------------------- |
| `remote/agent_client.py` | 新增       | TCP 客户端、拨号、断线重连          |
| `remote/handler.py`      | 新增       | 解析包 → 路由到 LocalExecutor / FileTransfer     |
| `remote/local_executor.py` | 改造自 `file.py` + `_execute_action`/`_execute_cmd` 部分 | 接收 cmd + TLV 参数 → 执行 → 返回字符串 |
| `remote/file_transfer.py` | 新增       | 文件传输：上传接收（写文件）+ 下载发送（读文件并分块），按 §3.6 协议 |
| `remote/main.py`         | 新增       | 无头守护进程（启动 → 拨号 → 心跳 → 处理协议包），无 CLI 交互 |

> **远程端不提供任何本地人工管理接口**：无本地 CLI、无本地 Web、无文件/命令面板。一切操作只能经由 C2 通过协议下发。

### 5.3 行为约束

- **无授权**：所有来自 C2 的合法连接指令**直接执行**，不做额外确认
- **无 LLM**：不调用任何大模型
- **不存储历史**：执行结果仅透传回 C2
- **本地路径处理**：路径解析在远程端本地完成，C2 不感知绝对路径
- **结果编码**：UTF-8 字符串；Shell 命令仍按系统编码（Windows: cp936；Linux: utf-8）抓取，子进程返回后强制转 UTF-8 上行
- **执行超时**：使用 `subprocess.run(..., timeout=cmd_timeout)`；超时返回 `Error: Command timed out after Ns`

### 5.4 拨号与重连

- 读取 `c2_address`（`host:port`）、`auth_token`、`agent_id`
- 启动后立即尝试连接；失败按指数退避重试（1s, 2s, 4s, ... 上限 60s）
- 连接成功后立即发送 `register` 包
- 每 `heartbeat_interval` 秒（默认 30s）发送 `heartbeat` 包
- 接收 `disconnect` 包或连接异常 → 清理 → 重连

---

## 6. 共享协议与工具

### 6.1 `common/protocol.py`

```python
PACKET_HEADER_LEN = 16

REQUEST_ID_OFFSET = 0    # 8B  uint64 LE
BODY_LEN_OFFSET   = 8    # 4B  uint32 LE
RESERVED_OFFSET   = 12   # 3B  reserved
CMD_OFFSET        = 15   # 1B  uint8

ACTION_CMDS = {
    0x01: ("list_dir",    ["path"]),
    0x02: ("make_dir",    ["path"]),
    0x03: ("delete_dir",  ["path"]),
    0x04: ("rename_dir",  ["path", "new_name"]),
    0x05: ("read_file",   ["path", "start_line", "end_line"]),
    0x06: ("write_file",  ["path", "content"]),
    0x07: ("delete_file", ["path"]),
    0x08: ("edit_file",   ["path", "operation", "start_line", "end_line", "content"]),
    0x09: ("rename_file", ["path", "new_name"]),
    0x0A: ("copy",        ["src", "dest"]),
    0x0B: ("move",        ["src", "dest"]),
    0x10: ("exec_cmd",    ["command"]),
}
CONTROL_CMDS = {0x80: "register", 0x81: "register_response", 0x82: "heartbeat", ...}

def encode_request(req_id: int, cmd: int, params: list[str]) -> bytes:
    """请求包：TLV 链式包身"""
def encode_response(req_id: int, cmd: int, result: str) -> bytes:
    """响应包：单一字符串包身（无 TLV 切分）"""
def decode_header(data: bytes) -> tuple[int, int, int]:
    """返回 (req_id, body_len, cmd)"""
def encode_tlv(params: list[str]) -> bytes: ...
def decode_tlv(buf: bytes) -> list[str]: ...
```

提供流式解码器 `PacketReader`：在 `asyncio.StreamReader` 之上逐包还原；处理半包、粘包；区分请求（TLV 解码）与响应（整段 UTF-8 字符串）。

### 6.2 TLV 编码

```python
def encode_tlv_chain(params: list) -> bytes:
    buf = b""
    for p in params:
        data = p.encode("utf-8") if isinstance(p, str) else p
        buf += len(data).to_bytes(4, "little") + data
    return buf

def decode_tlv_chain(buf: bytes) -> list[str]:
    out, i = [], 0
    while i < len(buf):
        n = int.from_bytes(buf[i:i+4], "little"); i += 4
        out.append(buf[i:i+n].decode("utf-8", errors="replace")); i += n
    return out
```

---

## 7. 配置

### 7.1 `config_c2.json`

```json
{
  "api_base": "http://localhost:11434/v1",
  "api_key": "ollama",
  "model": "llama3.2",
  "round_limit": 20,
  "cmd_timeout": 60,
  "auth_mode": 0,
  "system_prompt": "...{system_name} ...",

  "listen_host": "127.0.0.1",
  "listen_port": 8880,

  "c2_host": "0.0.0.0",
  "c2_port": 8881,
  "c2_auth_tokens": "token-for-server-01",
  "heartbeat_timeout_sec": 60
}
```

### 7.2 远程端配置（可选）

配置文件 `config_remote.json`（C2 与远程端**入口独立**，通过不同的 Python 模块区分，无需 `role` 字段）：

```json
{
  "c2_address": "192.168.1.100:8881",
  "agent_id": "server-01",
  "auth_token": "token-for-server-01",
  "heartbeat_interval_sec": 30,
  "cmd_timeout": 60,
  "reconnect_initial_sec": 1,
  "reconnect_max_sec": 60
}
```

**命令行参数覆盖**：远程端**所有配置项**均支持命令行传入，等效键名采用 kebab-case，优先级高于配置文件：

| 配置键                      | 命令行参数                   | 必填 | 说明                        |
| --------------------------- | ---------------------------- | ---- | --------------------------- |
| （配置文件路径）             | `--config <path>`            | 否   | 若指定，加载后作为默认值      |
| `c2_address`                | `--c2-address <host:port>`   | ✅   | C2 监听地址                  |
| `agent_id`                  | `--agent-id <id>`            | ✅   | 本机在 C2 侧的唯一标识       |
| `auth_token`                | `--auth-token <token>`       | ✅   | 与 C2 配置的 token 匹配      |
| `heartbeat_interval_sec`    | `--heartbeat-interval <sec>` | 否   | 默认 30                      |
| `cmd_timeout`               | `--cmd-timeout <sec>`        | 否   | 默认 60                     |
| `reconnect_initial_sec`     | `--reconnect-initial <sec>`  | 否   | 默认 1                       |
| `reconnect_max_sec`         | `--reconnect-max <sec>`      | 否   | 默认 60                      |

**优先级**：命令行参数 > 配置文件 > 代码内置默认值。

**支持无配置文件运行**：当 `--config` 未提供且所有必填项（`c2_address`、`agent_id`、`auth_token`）均通过命令行给出时，远程端可直接启动，不依赖任何文件。

---

## 8. 部署与运行流程

### 8.1 启动顺序

1. **远程端先启动**：开始拨号，等待 C2 接受
2. **C2 端后启动**（或不重启也可热接入）：监听端口，接收 register，校验 token，加入 AgentRegistry
3. **用户在 C2 CLI/Web 与 LLM 对话**：LLM 输出带 `target` 的 JSON，C2 转发给对应 Agent，等待结果回灌 LLM
4. **多远程端**：可同时接入 N 个 Agent；用户通过 `/target` 或 Web 下拉切换

### 8.2 典型场景

- **局域网**：C2 监听 `0.0.0.0:8881`；远程端填 `c2_address = 192.168.1.100:8881`
- **公网反向连接**（推荐）：远程端主动外连 C2 公网 IP:PORT；只需在 C2 侧路由器/防火墙开放一个入站端口
- **SSH 隧道**：`ssh -L 8881:localhost:8881 user@c2host`，远程端连 `localhost:8881`，全程加密

---

## 9. 安全考量

| 风险                    | 措施                                                       |
| ----------------------- | ---------------------------------------------------------- |
| 任意客户端注册          | 挑战-响应注册：远端持随机 nonce 注册，C2 回传 `sha256(nonce + c2_auth_tokens)`，远端本地校验后回传 `register_confirm` 才注册；token 不明文上线 |
| 协议重放                | 后续可加 nonce + HMAC（v1 仅静态 token，已记录为后续工作） |
| Agent 被劫持执行恶意命令 | 远程端本身已暴露，应部署在受信内网；v1 不做命令白名单     |
| C2 Web 未鉴权           | 默认 `127.0.0.1`；公网部署时建议 SSH 隧道或前置 Nginx 反代 |
| 凭据泄露                | token 写入配置文件，权限 600；握手后不再明文传输；可后续迁移至密钥文件 |
| 流量未加密              | ✅ 认证通过后已用 ChaCha20（§3.7）加密全部流量；如需更强可选叠加 TLS（`ssl_context`） |

> 原样例代码"免责声明"仍然适用：仅供个人/受信环境使用，未做生产级防护。

---

## 10. 后续可扩展（v2+ 候选）

1. **TLS**：可选叠加；AgentClient / NetworkServer 切换为 `ssl_context`（当前已用 ChaCha20 加密数据面，TLS 可再提供握手完整性）
2. ~~二进制上传/下载~~：**v1 已实现**（§3.6）。后续可加断点续传、压缩传输
3. ~~远程端本地 Web~~：明确**不提供**，远程端永远是无头进程（已从 v1 范围移除）
4. **C2 Web 鉴权**：Basic Auth / Token，用于公网部署
5. **命令白名单**：远程端可按 cmd + 参数前缀做 ACL
6. **多 C2 / Agent 集群**：当前每 C2 一进程；后续可做 Agent → 多 C2 灾备
7. **审计日志**：`audit.log` 记录所有动作、目标、结果哈希，便于事后追溯
8. **流式回传**：长任务进度推送（agent → c2 → web SSE），类似当前 chat_stream

---

## 11. 文件结构总览

```
XXX/
├── 设计文档.md                  # 本文档
├── README.md                    # 项目说明
├── requirements.txt             # 运行时依赖（含 pyinstaller）
├── config_c2.example.json
├── config_remote.example.json
├── build/                       # 编译脚本（PyInstaller 单文件产物）
│   ├── build.bat / build.sh            # C2 端编译
│   └── build_remote.bat / build_remote.sh  # Python 远程端编译
├── web/
│   └── index.html               # C2 Web UI
├── common/
│   └── protocol.py              # 协议常量、TLV 编解码、流式 PacketReader
├── src/                         # C2 端
│   ├── c2/
│   │   ├── network_server.py    # TCP 监听、鉴权、心跳 watchdog
│   │   ├── agent_registry.py    # Agent 状态与会话绑定
│   │   ├── forwarder.py         # 指令转发 + 控制包发送
│   │   └── file_transfer.py     # upload 发送 / download 接收（§3.6）
│   ├── llm.py                   # LLM 调用 + JSON 解析
│   ├── model.py                 # 改造：per-agent 会话
│   ├── command.py               # 改造：仅路由 + 安全检查
│   ├── controller.py            # 系统提示渲染 + 授权模式
│   ├── config.py
│   ├── web_server.py            # C2 Web：Agent 列表 + 切换 + 文件 API
│   └── main.py                  # C2 入口（CLI/Web）
├── remote/                      # 远程端（Python，独立包）
│   ├── agent_client.py          # 拨号、重连、心跳
│   ├── handler.py               # 包解析 + 路由 + 文件传输状态机
│   ├── local_executor.py        # 文件/Shell 执行
│   ├── file_transfer.py         # 数据包收发
│   └── main.py                  # 远程端入口
└── remote-c/                    # 远程端（C 版，零三方依赖，gcc 编译）
    ├── agent.h                  # 共享声明 + 协议常量 + 跨平台 socket 抽象
    ├── protocol.c               # 包头/TLV 编解码、流式包解析、wire I/O
    ├── actions.c                # 文件动作（list/read/write/edit/copy/...）
    ├── exec_cmd.c               # 跨平台 Shell 执行 + 超时
    ├── main.c                   # 配置/参数、拨号、注册、心跳、分发
    └── COMPILE.txt              # 编译指令与用法
```

---

## 12. 审核记录

| # | 审核点                                  | 用户反馈                                                   | 设计落地位置                          |
| - | --------------------------------------- | ---------------------------------------------------------- | ------------------------------------- |
| 1 | 协议形态                                | ✅ 确认合用（16B 头 + TLV 包身 + §3.6 数据包）            | §3.1, §3.6                            |
| 2 | 鉴权强度                                | ✅ 静态 token 够用                                         | §3.3 控制指令 (0x80/0x81), §4.6      |
| 3 | 多 Agent 切换语义                       | ✅ 会话 ↔ Agent 1:1 绑定，自动路由                          | §4.4, §4.7                            |
| 4 | LLM 输出 schema                         | ✅ 不新增远程端相关字段                                    | §4.7                                  |
| 5 | 远程端本地 Web                          | ✅ 不提供                                                  | §4.1, §5.1                            |
| 6 | 包身 type tag                           | ✅ 不需要，全 UTF-8 字符串                                 | §3.1                                  |
| 7 | 网络错误 vs 业务错误                     | ⚠️ **需区分**（详见下）                                    | §3.3 错误码与错误分类                  |
| 8 | 数据大小限制                            | ✅ 复用样例代码 `read_file` 50000 字符截断                  | §3.3 动作指令后注                     |
| 9 | 并发执行                                | ✅ 串行（保持现状）                                        | §4.3 Forwarder 注释                   |
| 10 | 运行模式                                | ✅ 完全独立入口，无 `--role`                                | §4.1, §5.1                            |

**第 7 项展开**：C2 端在 `Forwarder` 内按以下规则分类：
- 成功 / 业务错误（Agent 返回 `Error: ...`） → 透传至 LLM 会话历史
- 网络错误（超时 / 断连 / 协议异常） → 抛 `NetworkError`，ModelModule 终止当前多轮循环并向历史插入系统消息 `[Network Error: ...]`，AgentRegistry 标记 Agent 离线，UI 灰显该选项

---

## 13. 构建与 C 版远程端（已落地）

### 13.1 编译打包

`build/` 提供两套独立的 PyInstaller 编译脚本，产物为单文件可执行程序：

| 脚本 | 产物 | 说明 |
| ---- | ---- | ---- |
| `build/build.bat` / `build.sh` | `build/irudo_c2(.exe)` | C2 端，含 `web/` 数据目录，`--mode cli/web` 双模式 |
| `build/build_remote.bat` / `build_remote.sh` | `build/irudo_remote(.exe)` | Python 远程端（无第三方运行时依赖） |

- 编译依赖 PyInstaller（`requirements.txt` 已含；脚本在缺失时自动安装）
- 配置文件**不嵌入**可执行程序，运行时通过 `--config` 指定外部路径
- 原 `start_c2.*` / `start_remote.*` 启动脚本已移除，统一改为 `python -m` 或编译产物运行

### 13.2 C 版远程端（`remote-c/`）

零三方依赖的 C 实现，与 Python 远程端协议完全一致（§3、§3.6），win/linux 下 gcc 直接编译：

- **文件**：`agent.h`、`protocol.c`、`actions.c`、`exec_cmd.c`、`main.c`；编译指令见 `COMPILE.txt`
- **行为对齐**：挑战-响应注册握手（随机 nonce + sha256 校验 + register_confirm）、认证后 ChaCha20 全流量加密、心跳/重连（指数退避）、全部动作指令、upload/download 分包传输、`read_file` 50000 字符截断、`exec_cmd` 超时（Windows 下 cp936→UTF-8 转码）、错误以 `Error: ...` 前缀返回
- **参数**：`--c2-address` / `--agent-id` / `--auth-token`（必填）及 `--heartbeat-interval` / `--cmd-timeout` / `--reconnect-initial` / `--reconnect-max`（可选），并支持 `--config` 读取扁平 JSON 配置

---

**全部审核项已确认**，设计文档定稿。